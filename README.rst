SR Comp CLI
===========

A set of command line tools for accessing SR compstate repositories.

Note: A couple of these commands depend on the ``ruamel.yaml`` package,
which needs to be installed manually.
